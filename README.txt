Creates linear regression between variables-only works for one independent to one dependent variable.

Currently tries to improve weight and bias values until the end cost a consecutive pair of iterations is within 1 of each other. This value can be changed. There are also some options when loading in the dataset, it is possible to create a new list that is the difference between two lists.